VOLUME_PROMPT = """你是资深小说编辑。给定一批章节文本（编号按 001-500，当前批次为 {batch_start}-{batch_end}），每章内容已读入。
任务：
1) 识别该批次中属于同一“卷/大事件”的章节范围（连续或跨批次可续）。每个卷对应若干章节编号范围。
2) 对每个卷输出详细梗概，要求包含：起因、主要冲突、关键转折、高潮、结局/阶段性收束，以及主要人物动机与关系演变。
3) 如卷在结尾处未收束且可能与下一批续接，请标记“可能跨批次续接: 是”，并说明悬而未决的冲突点。
4) 输出结构化 JSON，示例：
[
  {{
    "volume_id": "V{{index}}",
    "chapters": ["{{start:03d}}-{{end:03d}}"],
    "may_continue_next_batch": true/false,
    "summary": "...（详述，便于后续情节重组和洗稿）",
    "key_events": ["事件1", "事件2", "..."],
    "characters": [
        {{"name": "...", "role": "...", "motivation": "...", "relations": "..."}}
    ]
  }}
]
请确保梗概前后逻辑清晰，便于后续洗稿（情节替换/顺序调整时仍能自洽）。
"""
CHAPTER_PROMPT = """你是资深小说编辑。给定某一卷的章节列表及全文内容。
任务：
1) 按卷梗概指示的章节范围，为每章提取章节梗概。
2) 每章梗概需包含：章节主事件、人物目标/动机、冲突与转折、当章留下的伏笔或悬念。
3) 输出结构化 JSON，示例：
[
  {{
    "chapter": "{chapter_no}",
    "summary": "...（详述且便于洗稿重组）",
    "beats": ["节奏点1", "节奏点2", "..."],
    "hooks": ["伏笔/悬念1", "..."]
  }}
]
保持逻辑连贯，便于后续情节顺序调整后仍自洽。
"""

WASH_PROMPT = """你是资深剧情策划，需要将多部小说的卷梗概与章节梗概洗稿，生成“逐章”新大纲。请遵循：

1) 必须按章节输出：逐章列出章节编号/标题、详细梗概、关键节奏点、伏笔/悬念、人物动机与关系变化。
2) 允许情节替换、顺序调整、主干/支线互换，但整体因果链要自洽：起因 → 冲突 → 转折 → 高潮 → 收束。
3) 人物动机与关系网要能支撑新的顺序与冲突走向。
4) 输出为可读文本（无需 JSON）。组织格式示例：
- 章节 C001：标题
  - 梗概：……
  - 节奏点：……
  - 伏笔/悬念：……
  - 人物动机与关系变化：……
- 章节 C002：标题
  …

保持前后逻辑连贯，即便调整顺序也要自洽。尽量覆盖全部素材中的信息，不要生成卷级摘要。
"""