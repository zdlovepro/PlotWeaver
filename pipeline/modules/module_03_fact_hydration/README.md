# 第三模块：按大纲补全事实

第三模块首先读取分层大纲，为每个关键事件、人物发展节点、因果连接、状态
变化和未决线索生成“事实需求”，再按照章节和段落位置回到原文取证。

它只补充后续图谱和生成真正需要的事实，不以覆盖所有句子、所有名词或所有
静态描述为目标。输出中的每条事实必须说明服务于哪个大纲节点以及为什么需要。

旧的“先无差别提取命题，再拼装事件”代码已经移出活动流水线，不得从本模块
入口调用。

